<?php
if (!defined('ABSPATH')) { exit; }

/**
 * Time helper (skeleton)
 *
 * Boundary (SSOT v1.5):
 * - Pure parse/format helpers only.
 * - MUST NOT generate week/month/quarter/year keys (business rules belong to core/time.php).
 */

// Intentionally left blank (skeleton).
