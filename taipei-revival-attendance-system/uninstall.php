<?php
/**
 * Uninstall cleanup (skeleton)
 *
 * NOTE:
 * - v1.x: keep conservative. Do NOT delete data by default unless explicitly decided in SSOT later.
 * - This file exists to satisfy WordPress uninstall entrypoint and future SSOT updates.
 */
if (!defined('WP_UNINSTALL_PLUGIN')) { exit; }

// Intentionally left blank (skeleton).
// Future: if SSOT decides data removal, read constants.php for table names and remove options safely.
